type: object
required:
  - id
  - type
properties:
  id:
    type: string
    example: "0x123...abc"
    pattern: '^0x[0-9a-fA-F]{64}$'
    description: User address string
  type:
    type: string
    enum: [ token ]
