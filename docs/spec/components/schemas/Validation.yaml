allOf:
  - $ref: '#/components/schemas/ValidationKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        required:
          - claims
        type: object
        properties:
          claims:
            type: array
            items:
              $ref: '#/components/schemas/Claim'