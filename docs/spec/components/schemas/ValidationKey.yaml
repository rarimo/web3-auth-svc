type: object
required:
  - id
  - type
properties:
  id:
    type: string
    example: "0x123...abc"
    pattern: '^0x[0-9a-fA-F]{40}$'
    description: User address
  type:
    type: string
    enum: [ validation ]
